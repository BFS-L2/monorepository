import { PageWrapper } from '@/components/ui/pageWrapper/PageWrapper'

import { LoginForm } from '@/features/login'

export const Login = () => {
	return (
		<PageWrapper>
			<LoginForm />
		</PageWrapper>
	)
}
